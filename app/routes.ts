import { type RouteConfig, index } from "@react-router/dev/routes";

/** Application route definitions consumed by React Router. */
const routes = [index("routes/home.tsx")] satisfies RouteConfig;

export default routes;
